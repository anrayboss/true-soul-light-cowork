# 音訊壓縮與 Whisper 轉寫優化指南

本指南整理了音訊壓縮位元率、編碼格式，以及如何兼顧「極小檔案大小」與「高準確度語音辨識（Whisper）」的設定知識，方便團隊在需要將音檔版控或傳輸時參考。

---

## 1. 為什麼音訊品質對 Whisper 轉寫有差？

*   **資訊遺失**：過度壓縮的音訊（例如低於 `16 kbps` 的普通 MP3）會損失大量的高頻與低頻細節，使人聲變得沙啞、有金屬電音或糊成一團，導致 Whisper 容易產生**漏字**、**錯字**，甚至因無意義雜訊而產生**幻覺**（不斷重複某些字詞）。
*   **重採樣機制**：Whisper 在輸入音訊前，會將其重採樣至 `16 kHz`。如果原始音訊位元率與採樣率過低，插值放大後的波形邊緣會極度模糊，辨識效果會大幅打折扣。
*   **音量實測結論**：經過實際測試（如 `TEST.mp4` 實測），**錄音音量即使僅有 1 格（微弱音量），模型依然能精準辨識**。這顯示對 Whisper 而言，**音軌的清晰度（無背景雜音、無過度壓縮失真）比起音量大小更為關鍵**。只要人聲乾淨、背景無雜訊，音量極小也不影響辨識；但若為完全靜音（0 格），則會因無音訊特徵而無法辨識，甚至可能產生幻覺。

---

## 2. 現代音訊格式比較 (MP3 vs. AAC vs. Opus)

在低位元率（Low Bitrate）的壓縮情境下，不同編碼器的效率有巨大世代差異：

| 格式 (編碼) | 世代與特點 | 低位元率表現 (24~32 kbps) | 適用於 Whisper 轉寫的建議位元率 |
| :--- | :--- | :--- | :--- |
| **MP3** | 舊世代 (1993) | **極差**。聲音會破碎、充滿電子雜音與空洞感。 | 96 kbps - 128 kbps 以上 |
| **AAC** | 中世代 (1997) | **普通**。比 MP3 好，但在極低位元率下仍有些許失真。 | 48 kbps - 64 kbps 以上 |
| **Opus** | 新世代 (2012) | **極佳**。專為網路語音與音樂串流設計，在此頻寬下人聲依然清晰。 | **24 kbps - 32 kbps** |

> **結論**：若要追求「最小檔案」與「最佳轉寫率」，**Opus** 是唯一的最佳解，其檔案大小可比 MP3 小 **3 至 4 倍**。

---

## 3. 最佳 Opus 壓縮參數設定指引

當使用音訊轉換工具（如 GoldWave、FFmpeg 或其他 GUI 工具）匯出 Opus 時，請採用以下黃金設定：

1.  **位元率 (Bitrate)**：設定為 **`24 kbps`**。
    *   *說明*：這是一般單人說話最經濟的位元率。如果音訊包含多人對談、背景有些許噪音，可微調至 **`32 kbps`** 確保品質。
2.  **應用程式類型 (Application)**：設定為 **`語音 (Voice / VOIP)`**，而非「音訊 (Audio)」。
    *   *說明*：此模式會使 Opus 針對人類語音頻段進行特化編碼，自動濾除無用的超高頻/超低頻背景噪音，能顯著提升 Whisper 的辨識精準度。
3.  **聲道 (Channels)**：務必選擇 **`單聲道 (Mono)`**。
    *   *說明*：這非常關鍵！如果是雙聲道，`24 kbps` 的頻寬會被平分（每聲道僅 `12 kbps`），音質會嚴重崩壞。單聲道則能獨佔完整的 `24 kbps` 頻寬。
4.  **VBR 模式 (Variable Bit Rate)**：設定為 **`開啟 (On / Enabled)`**。
    *   *說明*：可變位元率。當音訊中出現靜音或簡單音節時，編碼器會自動調低位元率以節省空間。
5.  **壓縮方式/複雜度 (Complexity)**：設定為 **`10`** (最高)。
    *   *說明*：此數值決定了壓縮演算法的複雜度。數值越高，編碼花費時間稍長，但能以最小的檔案達成最高的音質。

---

## 4. 關於專案 Git 版控的建議

*   **不建議直接版控大音檔**：Git 的設計並不適合追蹤大體積的二進位檔案，這會導致 `.git` 目錄迅速膨脹且無法輕易減肥。
*   **推薦做法**：
    *   **最佳方案**：將原始音影片上傳至雲端硬碟，Git 倉庫中只保留轉寫後的 `.txt`、`.srt` 與大綱 `.md` 檔案。
    *   **折衷方案**：如果因專案協作需求必須版控音檔，請務必先將音檔透過上述 **Opus 24 kbps Mono** 設定進行極度壓縮，以維持極小的檔案大小。

---

## 5. 常見問題：檔案大小/格式對轉錄時間的影響

*   **問題**：檔案大小（例如 MP3 vs. Opus）對 Whisper 轉錄時間有影響嗎？
*   **解答**：**幾乎沒有影響。**
    *   **轉錄時間的關鍵**：在於 Whisper 神經網路模型的推理（Inference）計算。這只與音訊的**「播放長度 (Duration)」**和使用的**「模型大小 (small/medium/large)」**有關，與檔案是 5MB 的 Opus 還是 50MB 的 MP3 無關。
    *   **解碼開銷可忽略**：大檔案在讀取與將音訊解碼為原始波形（PCM）時，只會多消耗零點幾秒的 CPU 時間，這在整體轉錄流程中微乎其微。

